# Development (with Vite proxy): use relative path
VITE_API_BASE_URL=/api/waste
# Must match PORT in the root .env (default backend port is 5000)
VITE_API_PROXY_TARGET=http://localhost:5000

# Production: point at your deployed API
# VITE_API_BASE_URL=https://your-api.example.com/api/waste
